// Notification fan-out: outbound email (SMTP) and webhook alerts (Slack/Discord).

export { formatBudgetAttribution } from "./budget-attribution";
export type { AlertChannelsInput } from "./save-alert-channels.usecase";
export * from "./send";
